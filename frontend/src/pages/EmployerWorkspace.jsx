import { useEffect, useMemo, useState } from 'react';
import { useLocation, useNavigate, useSearchParams } from 'react-router-dom';
import confetti from 'canvas-confetti';
import {
  AlertCircle, ArrowRight, BarChart3, Bell, BriefcaseBusiness, Building2, CalendarDays, Check,
  CheckCircle2, ChevronRight, CircleDollarSign, ClipboardList, FileText, Globe2, LayoutDashboard,
  Menu, MessageCircle, Moon, Plus, Search, Settings, Sparkles, Star, Sun, Target, Trash2, UserCheck, Users, X
} from 'lucide-react';
import api from '../services/api';
import { useAuth } from '../context/AuthContext';
import CompanyReviews from '../components/company/CompanyReviews';
import CompanyQA from '../components/company/CompanyQA';
import TopCandidatesList from '../components/employer/TopCandidatesList';
import DashboardMetricCards from '../components/employer/DashboardMetricCards';
import ApplicationsTable from '../components/employer/ApplicationsTable';

const stages = [
  ['overview', 'Dashboard', LayoutDashboard], ['profile', 'Company & Legal', Building2], ['post', 'Post Job', Plus],
  ['jobs', 'My Jobs', BriefcaseBusiness], ['applications', 'Applications', ClipboardList], ['matching', 'AI Matching', Target],
  ['shortlist', 'Shortlist', Star], ['interviews', 'Interviews', CalendarDays], ['hired', 'Hire & Onboarding', UserCheck], ['reviews', 'Reviews Management', MessageCircle], ['summary', 'AI Candidate Summary', Sparkles]
];
const stageLabelsAm = { overview: 'ዳሽቦርድ', profile: 'የድርጅት መገለጫ', post: 'ስራ ለጥፍ', jobs: 'የእኔ ስራዎች', applications: 'ማመልከቻዎች', matching: 'AI ማዛመጃ', shortlist: 'የተመረጡ', interviews: 'ቃለ መጠይቆች', hired: 'ቅጥር እና ማስጀመሪያ', reviews: 'የግምገማ አስተዳደር', summary: 'AI የእጩ ማጠቃለያ' };
const seedJobs = [
  { id: 'demo-1', title: 'Senior React Developer', department: 'Engineering', location: 'Addis Ababa / Hybrid', work_mode: 'hybrid', job_type: 'full-time', status: 'published', applicantsCount: 24, shortlisted: 6, salary_min: 90000, salary_max: 140000, currency: 'ETB', description: 'Build reliable, accessible product experiences with React and Node.js.' },
  { id: 'demo-2', title: 'Product Designer', department: 'Product', location: 'Remote - Ethiopia', work_mode: 'remote', job_type: 'full-time', status: 'published', applicantsCount: 18, shortlisted: 4, salary_min: 70000, salary_max: 110000, currency: 'ETB', description: 'Shape a design system and delightful candidate experiences.' },
  { id: 'demo-3', title: 'Talent Acquisition Intern', department: 'People', location: 'Addis Ababa', work_mode: 'on-site', job_type: 'internship', status: 'draft', applicantsCount: 0, shortlisted: 0, salary_min: 0, salary_max: 0, currency: 'ETB', description: 'Support sourcing, screening, and employer brand initiatives.' }
];
const seedApplications = [
  { id: 'a-1', name: 'Mekdes Tadesse', email: 'mekdes@example.com', jobTitle: 'Senior React Developer', status: 'shortlisted', matchScore: 94, skills: ['React', 'TypeScript', 'Node.js', 'Testing'], experience: '5 years', education: 'BSc Computer Science' },
  { id: 'a-2', name: 'Samuel Bekele', email: 'samuel@example.com', jobTitle: 'Senior React Developer', status: 'under-review', matchScore: 87, skills: ['React', 'JavaScript', 'Express', 'AWS'], experience: '4 years', education: 'BSc Software Engineering' },
  { id: 'a-3', name: 'Hanna Alemu', email: 'hanna@example.com', jobTitle: 'Product Designer', status: 'applied', matchScore: 78, skills: ['Figma', 'UX Research', 'Prototyping'], experience: '3 years', education: 'BA Design' },
  { id: 'a-4', name: 'Dawit Girma', email: 'dawit@example.com', jobTitle: 'Product Designer', status: 'interview-scheduled', matchScore: 91, skills: ['Figma', 'Design Systems', 'Research'], experience: '6 years', education: 'BA Fine Arts' },
  { id: 'a-5', name: 'Rahel Tesfaye', email: 'rahel@example.com', jobTitle: 'Talent Acquisition Intern', status: 'hired', matchScore: 89, skills: ['Sourcing', 'Communication', 'Excel'], experience: '1 year', education: 'BA Human Resources' }
];
const blankJob = { title: '', department: 'Engineering', job_type: 'full-time', work_mode: 'hybrid', location: '', salary_min: '', salary_max: '', currency: 'ETB', application_deadline: '', required_skills: '', description: '' };

function ScoreRing({ score, size = 54 }) {
  const color = score >= 85 ? '#10b981' : score >= 65 ? '#f59e0b' : '#ef4444';
  return <div className="relative flex shrink-0 items-center justify-center rounded-full" style={{ width: size, height: size, background: `conic-gradient(${color} ${score * 3.6}deg, #e2e8f0 0deg)` }}><div className="flex items-center justify-center rounded-full bg-white font-black text-slate-800" style={{ width: size - 10, height: size - 10, fontSize: size < 60 ? 12 : 16 }}>{score}%</div></div>;
}
function Toast({ toast, onClose }) { return toast && <div className="fixed bottom-5 right-5 z-[70] flex max-w-sm items-center gap-3 rounded-2xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white shadow-2xl"><CheckCircle2 className="h-5 w-5 text-emerald-400" />{toast}<button onClick={onClose} className="ml-2 text-slate-400"><X className="h-4 w-4" /></button></div>; }
function Field({ label, children }) { const isDark = String(children?.props?.className || '').includes('bg-slate-900'); return <div className="block min-w-0"><span className="mb-2 block text-xs font-black uppercase tracking-wide" style={{ display: 'block', color: isDark ? '#f8fafc' : '#0f172a', fontSize: '0.75rem', lineHeight: '1rem', fontWeight: 800 }}>{label}</span>{children}</div>; }
function inputClass(dark) { return `min-h-11 w-full min-w-0 rounded-xl border px-3.5 py-3 text-sm font-medium text-slate-900 outline-none transition placeholder:font-medium placeholder:text-slate-500 focus:border-blue-600 focus:ring-4 focus:ring-blue-500/10 ${dark ? 'border-slate-700 bg-slate-900 text-white placeholder:text-slate-400' : 'border-slate-300 bg-white shadow-sm hover:border-slate-400'}`; }

export default function EmployerWorkspace() {
  const { user, token, logout } = useAuth();
  const location = useLocation();
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const [active, setActive] = useState(() => location.pathname.includes('/post-job') ? 'post' : searchParams.get('view') || 'overview');
  const [jobs, setJobs] = useState([]);
  const [applications, setApplications] = useState([]);
  const [interviews, setInterviews] = useState([]);
  const [company, setCompany] = useState({ company_name: '', industry: '', company_size: '11-50', location: '', website: '', description: '', mission: '', vision: '', values: '', benefits: '', license_number: '', tin_number: '', license_expiry_date: '', verification_status: 'Under Review' });
  const [job, setJob] = useState(blankJob);
  const [editingJobId, setEditingJobId] = useState(null);
  const [wizard, setWizard] = useState(1);
  const [selected, setSelected] = useState(null);
  const [aiCandidate, setAiCandidate] = useState(null);
  const [matchingJobId, setMatchingJobId] = useState('');
  const [search, setSearch] = useState('');
  const [status, setStatus] = useState('all');
  const [minScore, setMinScore] = useState(0);
  const [toast, setToast] = useState('');
  const [dark, setDark] = useState(false);
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const [loading, setLoading] = useState(false);
  const [showSchedule, setShowSchedule] = useState(false);
  const [schedule, setSchedule] = useState({ date: '', time: '', type: 'video', link: '', notes: '' });
  const [language, setLanguage] = useState(() => localStorage.getItem('employerLanguage') || 'EN');
  const languageOptions = [
    ['EN', 'English'],
    ['AM', 'አማርኛ'],
    ['OM', 'Afaan Oromoo'],
    ['TI', 'ትግርኛ'],
  ];
  const isAmharic = language === 'AM';
  const labelForStage = (id, english) => isAmharic ? stageLabelsAm[id] : english;
  const changeLanguage = (event) => {
    const nextLanguage = event.target.value;
    setLanguage(nextLanguage);
    localStorage.setItem('employerLanguage', nextLanguage);
  };

  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(''), 3000); };
  const validateJob = () => {
    const missing = [['Job title', job.title], ['Location', job.location], ['Description', job.description]]
      .filter(([, value]) => !String(value || '').trim())
      .map(([label]) => label);
    if (missing.length) {
      notify(`Please complete: ${missing.join(', ')}`);
      setWizard(1);
      return false;
    }
    return true;
  };
  useEffect(() => {
    const handleScheduleClick = (event) => {
      const buttonLabel = event.target.closest('button')?.textContent?.trim();
      if (buttonLabel === 'Save as Draft' || buttonLabel === 'Publish Job Now') {
        event.preventDefault();
        event.stopImmediatePropagation();
        if (!loading && validateJob()) saveJob(buttonLabel === 'Publish Job Now' ? 'published' : 'draft');
        return;
      }
      if (buttonLabel !== 'Schedule Post') return;
      event.preventDefault();
      event.stopImmediatePropagation();
      const selectedDate = window.prompt('Enter the publication date and time (YYYY-MM-DDTHH:MM):');
      if (selectedDate) saveJob('scheduled', selectedDate);
    };
    document.addEventListener('click', handleScheduleClick, true);
    return () => document.removeEventListener('click', handleScheduleClick, true);
  });
  useEffect(() => {
    const requestedStage = location.pathname.includes('/post-job') ? 'post' : searchParams.get('view') || 'overview';
    if (stages.some(([id]) => id === requestedStage)) setActive(requestedStage);
  }, [location.pathname, searchParams]);
  const selectStage = (stage) => {
    setActive(stage);
    setSidebarOpen(false);
    const nextUrl = stage === 'overview' ? '/employer-dashboard' : `/employer-dashboard?view=${stage}`;
    navigate(nextUrl, { replace: true });
  };
  const navigateFromMetric = (targetTab, subTab) => {
    const stageMap = { my_jobs: 'jobs', ai_matching: 'matching' };
    const stage = stageMap[targetTab] || targetTab;
    if (stage === 'matching') setMinScore(subTab === 'top_matches' ? 80 : 0);
    if (stage === 'applications') setStatus('all');
    selectStage(stage);
  };
  useEffect(() => {
    const metricTargets = { 'Active Jobs': ['jobs', 'published'], Applicants: ['applications', 'all'], 'High AI Matches': ['matching', 'top_matches'], Shortlisted: ['shortlist', 'all'], Interviews: ['interviews', 'upcoming'], Hired: ['hired', 'confirmed'] };
    const handleMetricClick = (event) => {
      if (active !== 'overview') return;
      const cardElement = event.target.closest('div.rounded-2xl.border.p-4');
      const label = cardElement?.querySelector('p')?.textContent?.trim();
      const target = metricTargets[label];
      if (target) navigateFromMetric(target[0], target[1]);
    };
    document.addEventListener('click', handleMetricClick);
    return () => document.removeEventListener('click', handleMetricClick);
  }, [active, applications, jobs, interviews]);
  useEffect(() => {
    let mounted = true;
    api.get('/employer/profile').then(({ data }) => {
      if (!mounted) return;
      setCompany((current) => ({ ...current, ...data }));
    }).catch(() => {}).finally(() => mounted && setLoading(false));
    api.get('/employer/jobs').then(async ({ data }) => {
      if (!mounted) return;
      const ownedJobs = data.jobs || data || [];
      setJobs(ownedJobs);
      if (ownedJobs.length) setMatchingJobId((current) => current || String(ownedJobs[0].id));
      const applicationGroups = await Promise.all(ownedJobs.map((item) => api.get(`/employer/jobs/${item.id}/applications`).then((response) => response.data.applicants || []).catch(() => [])));
      if (mounted) setApplications(applicationGroups.flat().map((item) => ({ ...item, id: item.applicationId, name: item.name, email: item.contact, jobTitle: ownedJobs.find((jobItem) => String(jobItem.id) === String(item.job_id))?.title || item.jobTitle, matchScore: Number(item.matchPercentage || 0), status: item.status })));
    }).catch(() => {});
    return () => { mounted = false; };
  }, [token]);
  const filtered = useMemo(() => applications.filter((item) => (!search || `${item.name} ${item.jobTitle}`.toLowerCase().includes(search.toLowerCase())) && (status === 'all' || item.status === status) && Number(item.matchScore || 0) >= minScore), [applications, search, status, minScore]);
  const stats = { active: jobs.filter((item) => item.status === 'published').length, applicants: applications.length, high: applications.filter((item) => item.matchScore >= 80).length, shortlisted: applications.filter((item) => item.status === 'shortlisted').length, interviews: interviews.length + applications.filter((item) => item.status === 'interview-scheduled').length, hired: applications.filter((item) => item.status === 'hired').length };
  const updateApplication = async (id, nextStatus) => { setApplications((current) => current.map((item) => item.id === id ? { ...item, status: nextStatus } : item)); try { if (nextStatus === 'shortlisted') await api.patch(`/applications/${id}/shortlist`); else if (nextStatus === 'hired') await api.patch(`/applications/${id}/hire`); else if (nextStatus === 'rejected') await api.patch(`/applications/${id}/reject`); } catch (error) { notify(error?.response?.data?.message || 'Unable to update candidate.'); return; } notify(`Candidate moved to ${nextStatus}`); };
  const saveCompany = async (event) => { event.preventDefault(); try { await api.put('/employer/profile', company); notify('Company profile saved'); } catch (error) { notify(error?.response?.data?.message || 'Unable to save company profile.'); } };
  const enhanceJob = () => setJob((current) => ({ ...current, description: `Responsibilities:\nâ€¢ Own high-quality ${current.title || 'product'} delivery from discovery to launch.\nâ€¢ Collaborate with cross-functional teams and document decisions.\nâ€¢ Improve reliability, accessibility, and measurable user outcomes.\n\nScreening questions:\n1. Tell us about a similar project you shipped.\n2. How do you balance speed and quality?` }));
  const saveJob = async (nextStatus = 'draft', scheduledAt = null) => { const payload = { ...job, description: job.description || '', sector: job.department, jobType: job.job_type, workMode: job.work_mode, salaryMin: job.salary_min, salaryMax: job.salary_max, applicationDeadline: job.application_deadline, requiredSkills: job.required_skills }; setLoading(true); try { const response = editingJobId ? await api.put(`/jobs/${editingJobId}`, payload) : await api.post('/jobs', payload); let savedJob = response.data; if (nextStatus === 'published') savedJob = (await api.patch(`/jobs/${savedJob.id}/publish`)).data; if (nextStatus === 'scheduled') savedJob = (await api.patch(`/jobs/${savedJob.id}/schedule`, { scheduledAt })).data; setJobs((current) => [savedJob, ...current.filter((item) => String(item.id) !== String(savedJob.id))]); localStorage.setItem('employerJobs', JSON.stringify([savedJob, ...JSON.parse(localStorage.getItem('employerJobs') || '[]').filter((item) => String(item.id) !== String(savedJob.id))])); setJob(blankJob); setEditingJobId(null); setWizard(1); setActive('jobs'); notify(nextStatus === 'published' ? 'Job published successfully' : nextStatus === 'scheduled' ? 'Job scheduled successfully' : 'Draft saved'); if (nextStatus === 'published') confetti({ particleCount: 120, spread: 70, origin: { y: 0.65 } }); } catch (error) { notify(error?.response?.data?.message || 'Unable to save job.'); } finally { setLoading(false); } };
  const toggleJob = async (item) => { const next = item.status === 'published' ? 'paused' : 'published'; setJobs((current) => current.map((jobItem) => jobItem.id === item.id ? { ...jobItem, status: next } : jobItem)); try { await api.patch(`/employer/jobs/${item.id}/status`, { status: next }); } catch {} notify(`Job ${next}`); };
  const deleteJob = async (id) => { setJobs((current) => current.filter((item) => item.id !== id)); try { await api.delete(`/employer/jobs/${id}`); } catch {} notify('Job deleted'); };
  const editJob = (item) => { setEditingJobId(item.id); setJob({ title: item.title || '', department: item.department || item.category || 'Engineering', job_type: item.job_type || 'full-time', work_mode: item.work_mode || 'hybrid', location: item.location || '', salary_min: item.salary_min || '', salary_max: item.salary_max || '', currency: item.currency || 'ETB', application_deadline: item.application_deadline || '', required_skills: (item.required_skills || item.tags || []).join ? (item.required_skills || item.tags || []).join(', ') : item.required_skills || '', description: item.description || item.fullDescription || '' }); setWizard(1); setActive('post'); notify('Job loaded for editing'); };
  const scheduleInterview = async (event) => { event.preventDefault(); if (!selected) return; const item = { applicationId: selected.id, candidateId: selected.candidateId, scheduledDate: schedule.date, scheduledTime: schedule.time, interviewType: schedule.type, meetingLink: schedule.link, notes: schedule.notes }; try { await api.post('/interviews', item); setInterviews((current) => [...current, { ...item, candidate_name: selected.name, job_title: selected.jobTitle, scheduled_at: `${schedule.date}T${schedule.time}` }]); updateApplication(selected.id, 'interview-scheduled'); setShowSchedule(false); notify('Interview scheduled'); } catch (error) { notify(error?.response?.data?.message || 'Unable to schedule interview.'); } };
  const hire = (item) => { updateApplication(item.id, 'hired'); confetti({ particleCount: 160, spread: 90, origin: { y: 0.6 } }); notify(`${item.name} marked as hired`); };

  const shell = dark ? 'employer-workspace bg-slate-950 text-slate-100' : 'employer-workspace bg-[#f7f9fc] text-slate-900';
  const card = dark ? 'border-slate-800 bg-slate-900' : 'border-slate-200 bg-white';
  const employeeName = user?.full_name || user?.name || 'Employee';
  const title = active === 'overview' ? (isAmharic ? `እንኳን ወደ ዳሽቦርድ በደህና መጡ፣ ${employeeName}` : `Welcome, ${employeeName}`) : labelForStage(active, stages.find(([id]) => id === active)?.[1] || 'Dashboard');
  const companyName = company.company_name || user?.full_name || 'Your Company';
  const renderApplications = (items = filtered) => active === 'matching' ? <TopCandidatesList jobId={matchingJobId} onSelectCandidate={setAiCandidate} onShortlist={(candidate) => updateApplication(candidate.applicationId, 'shortlisted')} onSchedule={(candidate) => { setSelected({ ...candidate, id: candidate.applicationId, candidateId: candidate.candidateId }); setShowSchedule(true); }} /> : <div className={`overflow-hidden rounded-2xl border ${card}`}><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-100 text-xs uppercase tracking-wider text-slate-950 dark:border-slate-700 dark:bg-slate-800 dark:text-white"><tr><th className="px-5 py-4 font-black">Candidate</th><th className="font-black">Role</th><th className="font-black">AI Score</th><th className="font-black">Status</th><th className="px-5 font-black">Actions</th></tr></thead><tbody className="divide-y divide-slate-100 dark:divide-slate-800">{items.map((item) => { const statusClass = String(item.status || 'under-review').toLowerCase().replace(/\s+/g, '-'); return <tr key={item.id} className="hover:bg-slate-50/70 dark:hover:bg-slate-800/50"><td className="px-5 py-4"><button onClick={() => setSelected(item)} className="text-left font-bold text-blue-700 hover:underline dark:text-blue-300">{item.name}</button><p className="text-xs font-semibold text-slate-700 dark:text-slate-300">{item.email}</p></td><td className="font-bold text-slate-900 dark:text-slate-100">{item.jobTitle}</td><td><button onClick={() => setSelected(item)} className="flex items-center gap-2"><ScoreRing score={item.matchScore} size={42} /><span className="font-black text-slate-900 dark:text-white">{item.matchScore}%</span></button></td><td><span className={`rounded-full px-3 py-1 text-xs font-bold capitalize ${statusClass === 'hired' || statusClass === 'completed' ? 'border border-emerald-300 bg-emerald-100 text-emerald-950 dark:border-emerald-700 dark:bg-emerald-950 dark:text-emerald-200' : statusClass === 'shortlisted' || statusClass === 'interview-scheduled' || statusClass === 'active' ? 'border border-blue-300 bg-blue-100 text-blue-950 dark:border-blue-700 dark:bg-blue-950 dark:text-blue-200' : 'border border-amber-300 bg-amber-100 text-amber-950 dark:border-amber-700 dark:bg-amber-950 dark:text-amber-200'}`}>{item.status.replaceAll('-', ' ')}</span></td><td className="px-5"><div className="flex gap-2"><button onClick={() => updateApplication(item.id, 'shortlisted')} className="rounded-lg border border-emerald-300 bg-emerald-100 p-2 text-emerald-800 dark:border-emerald-700 dark:bg-emerald-950 dark:text-emerald-200" title="Shortlist"><Check className="h-4 w-4" /></button><button onClick={() => { setSelected(item); setShowSchedule(true); }} className="rounded-lg border border-violet-300 bg-violet-100 p-2 text-violet-800 dark:border-violet-700 dark:bg-violet-950 dark:text-violet-200" title="Schedule"><CalendarDays className="h-4 w-4" /></button></div></td></tr>})}{!items.length && <tr><td colSpan="5" className="px-5 py-12 text-center text-slate-700 dark:text-slate-300 font-bold">No candidate matches the current filters.</td></tr>}</tbody></table></div></div>;

  return <div className={`min-h-screen ${shell}`}><select value={language} onChange={changeLanguage} className="fixed right-4 top-28 z-30 rounded-xl border-2 border-blue-200 bg-blue-600 px-3 py-2 text-xs font-black text-white shadow-lg outline-none hover:bg-blue-700 sm:top-32" aria-label="Change language">{languageOptions.map(([value, label]) => <option key={value} value={value} className="bg-white font-semibold text-slate-900">{label}</option>)}</select>
    <header className={`sticky top-0 z-40 flex h-20 items-center justify-between border-b px-4 sm:px-6 ${card}`}><div className="flex items-center gap-3"><button onClick={() => setSidebarOpen(true)} className="rounded-xl p-2 hover:bg-slate-100 lg:hidden"><Menu className="h-5 w-5" /></button><div className="rounded-xl bg-blue-600 p-2 text-white"><Sparkles className="h-5 w-5" /></div><div><p className="text-xs font-bold uppercase tracking-[.2em] text-blue-600">Employer workspace</p><h1 className="text-lg font-black">SmartRecruit AI</h1></div></div><div className="flex items-center gap-2 sm:gap-4"><div className="hidden items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm text-slate-400 md:flex"><Search className="h-4 w-4" />Search <kbd className="rounded bg-slate-100 px-1.5 text-[10px]">âŒ˜ K</kbd></div><button className="relative rounded-xl p-2 hover:bg-slate-100"><Bell className="h-5 w-5" /><span className="absolute right-1 top-1 h-2 w-2 rounded-full bg-red-500" /></button><button onClick={() => setDark((value) => !value)} className="rounded-xl p-2 hover:bg-slate-100">{dark ? <Sun className="h-5 w-5" /> : <Moon className="h-5 w-5" />}</button><div className="hidden items-center gap-2 border-l pl-4 sm:flex"><div className="flex h-9 w-9 items-center justify-center rounded-full bg-blue-100 font-black text-blue-700">{companyName[0]}</div><span className="max-w-32 truncate text-sm font-bold">{companyName}</span></div><button onClick={logout} className="rounded-xl bg-slate-900 px-3 py-2 text-xs font-bold text-white">Logout</button></div></header>
    <div className="flex">{sidebarOpen && <button type="button" aria-label="Close navigation" onClick={() => setSidebarOpen(false)} className="fixed inset-0 z-40 bg-slate-950/40 lg:hidden" />}<aside className={`fixed inset-y-0 left-0 z-50 w-72 transform border-r p-5 pt-24 transition-transform lg:sticky lg:top-20 lg:h-[calc(100vh-5rem)] lg:translate-x-0 ${card} ${sidebarOpen ? 'translate-x-0' : '-translate-x-full'}`}><button onClick={() => setSidebarOpen(false)} className="absolute right-4 top-5 lg:hidden"><X /></button><p className="mb-4 px-3 text-xs font-black uppercase tracking-widest text-slate-400">Recruitment pipeline</p><nav className="space-y-1">{stages.map(([id, label, Icon], index) => <button key={id} type="button" onClick={() => selectStage(id)} aria-current={active === id ? 'page' : undefined} title={`Open ${label}`} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-bold transition ${active === id ? 'bg-blue-600 text-white shadow-lg shadow-blue-600/20' : 'text-slate-500 hover:bg-slate-100 hover:text-slate-900'}`}><span className="flex h-6 w-6 items-center justify-center rounded-lg bg-black/5 text-xs">{index + 1}</span><Icon className="h-4 w-4" />{label}{id === 'applications' && <span className="ml-auto rounded-full bg-red-100 px-2 py-0.5 text-[10px] text-red-700">{applications.length}</span>}</button>)}</nav><div className="mt-8 rounded-2xl bg-blue-50 p-4"><p className="text-xs font-bold text-blue-700">AI matching engine</p><p className="mt-1 text-xs leading-5 text-blue-600">Rank candidates using skills, experience, education, and fit.</p></div></aside><main className="min-w-0 flex-1 p-4 sm:p-6 lg:p-10"><div className="mx-auto max-w-7xl"><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-bold text-blue-600">Stage {stages.findIndex(([id]) => id === active) + 1} of 9</p><h2 className="mt-1 text-3xl font-black tracking-tight">{title}</h2><p className="mt-2 text-sm text-slate-500">Operate your hiring pipeline from one intelligent workspace.</p></div>{active !== 'post' && <button onClick={() => selectStage('post')} className="flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-3 text-sm font-bold text-white shadow-lg shadow-blue-600/20"><Plus className="h-4 w-4" /> Post New Job</button>}</div>

      {active === 'overview' && <><div className="mb-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-6">{[['Active Jobs', stats.active, BriefcaseBusiness, 'text-blue-600'], ['Applicants', stats.applicants, Users, 'text-emerald-600'], ['High AI Matches', stats.high, Target, 'text-violet-600'], ['Shortlisted', stats.shortlisted, Star, 'text-amber-600'], ['Interviews', stats.interviews, CalendarDays, 'text-pink-600'], ['Hired', stats.hired, UserCheck, 'text-cyan-600']].map(([label, value, Icon, color]) => <div className={`rounded-2xl border p-4 shadow-sm ${card}`} key={label}><div className="flex items-center justify-between"><p className="text-xs font-bold text-slate-400">{label}</p><Icon className={`h-5 w-5 ${color}`} /></div><p className="mt-3 text-3xl font-black">{value}</p><p className="mt-1 text-xs text-emerald-600">Live pipeline metric</p></div>)}</div><div className={`rounded-2xl border p-5 shadow-sm ${card}`}><div className="mb-5 flex items-center justify-between"><div><h3 className="text-lg font-black">Recent Applications</h3><p className="text-sm text-slate-500">The strongest candidates across your open roles.</p></div><button onClick={() => setActive('applications')} className="text-sm font-bold text-blue-600">View all <ArrowRight className="inline h-4 w-4" /></button></div>{renderApplications(applications.slice(0, 4))}</div></>}
  {active === 'overview' && <><div className="mb-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-6">{[['Active Jobs', stats.active, BriefcaseBusiness, 'text-blue-600'], ['Applicants', stats.applicants, Users, 'text-emerald-600'], ['High AI Matches', stats.high, Target, 'text-violet-600'], ['Shortlisted', stats.shortlisted, Star, 'text-amber-600'], ['Interviews', stats.interviews, CalendarDays, 'text-pink-600'], ['Hired', stats.hired, UserCheck, 'text-cyan-600']].map(([label, value, Icon, color]) => <div className={`rounded-2xl border p-4 shadow-sm ${card}`} key={label}><div className="flex items-center justify-between"><p className="text-xs font-black uppercase tracking-wide text-slate-700 dark:text-slate-300">{label}</p><Icon className={`h-5 w-5 ${color}`} /></div><p className="mt-3 text-3xl font-black text-slate-950 dark:text-white">{value}</p><p className="mt-1 text-xs font-bold text-emerald-600 dark:text-emerald-400">Live pipeline metric</p></div>)}</div><div className={`rounded-2xl border p-5 shadow-sm ${card}`}><div className="mb-5 flex items-center justify-between"><div><h3 className="text-lg font-black text-slate-950 dark:text-white">Recent Applications</h3><p className="text-sm font-medium text-slate-600 dark:text-slate-300">The strongest candidates across your open roles.</p></div><button onClick={() => setActive('applications')} className="text-sm font-bold text-blue-600">View all <ArrowRight className="inline h-4 w-4" /></button></div>{renderApplications(applications.slice(0, 4))}</div></>}

      {active === 'profile' && <form onSubmit={saveCompany} className={`space-y-6 rounded-2xl border p-6 shadow-sm ${card}`}><div className="grid gap-5 md:grid-cols-2"><Field label="Representative Full Name"><input className={inputClass(dark)} value={company.representative_name || user?.full_name || ''} onChange={(e) => setCompany({ ...company, representative_name: e.target.value })} /></Field><Field label="Position / Job Title"><input className={inputClass(dark)} value={company.representative_title || ''} onChange={(e) => setCompany({ ...company, representative_title: e.target.value })} placeholder="HR Manager" /></Field><Field label="Work Email"><input type="email" className={inputClass(dark)} value={company.work_email || user?.email || ''} onChange={(e) => setCompany({ ...company, work_email: e.target.value })} /></Field><Field label="Phone Number"><input className={inputClass(dark)} value={company.phone || ''} onChange={(e) => setCompany({ ...company, phone: e.target.value })} /></Field><Field label="Company Name"><input required className={inputClass(dark)} value={company.company_name} onChange={(e) => setCompany({ ...company, company_name: e.target.value })} /></Field><Field label="Industry"><input className={inputClass(dark)} value={company.industry} onChange={(e) => setCompany({ ...company, industry: e.target.value })} /></Field><Field label="Company Size"><select className={inputClass(dark)} value={company.company_size} onChange={(e) => setCompany({ ...company, company_size: e.target.value })}><option>1-10</option><option>11-50</option><option>51-200</option><option>201-500</option><option>500+</option></select></Field><Field label="HQ Location"><input className={inputClass(dark)} value={company.location} onChange={(e) => setCompany({ ...company, location: e.target.value })} /></Field><Field label="Website"><input type="url" className={inputClass(dark)} value={company.website} onChange={(e) => setCompany({ ...company, website: e.target.value })} /></Field><Field label="Social Media Links"><input className={inputClass(dark)} value={company.social_links || ''} onChange={(e) => setCompany({ ...company, social_links: e.target.value })} placeholder="LinkedIn, Facebook, X" /></Field></div><div className="grid gap-5 md:grid-cols-2">{[['description', 'About Company'], ['mission', 'Mission'], ['vision', 'Vision'], ['values', 'Values tags'], ['benefits', 'Perks / Benefits']].map(([key, label]) => <Field key={key} label={label}><textarea rows="3" className={inputClass(dark)} value={company[key] || ''} onChange={(e) => setCompany({ ...company, [key]: e.target.value })} /></Field>)}</div><div className="rounded-2xl border border-dashed border-blue-200 bg-blue-50/50 p-5"><div className="mb-4 flex items-center gap-2"><FileText className="h-5 w-5 text-blue-600" /><h3 className="font-black">Legal Verification Desk</h3><span className="ml-auto rounded-full bg-amber-100 px-3 py-1 text-xs font-bold text-amber-700">{company.verification_status}</span></div><div className="grid gap-5 md:grid-cols-3"><Field label="License / Registration Number"><input className={inputClass(dark)} value={company.license_number} onChange={(e) => setCompany({ ...company, license_number: e.target.value })} /></Field><Field label="TIN Number"><input className={inputClass(dark)} value={company.tin_number} onChange={(e) => setCompany({ ...company, tin_number: e.target.value })} /></Field><Field label="License Expiry Date"><input type="date" className={inputClass(dark)} value={company.license_expiry_date} onChange={(e) => setCompany({ ...company, license_expiry_date: e.target.value })} /></Field></div><div className="mt-5"><Field label="Trade License Document"><input type="file" accept=".pdf,.png,.jpg,.jpeg" className={`${inputClass(dark)} border-dashed`} /></Field></div><p className="mt-3 flex items-center gap-2 text-xs text-slate-500"><AlertCircle className="h-4 w-4" />Expiry validity is checked as you update the date. Platform approval controls the final badge.</p></div><button className="rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white">Save Company Profile</button></form>}

      {active === 'post' && <div className="grid gap-6 xl:grid-cols-[1.25fr_.75fr]"><div className={`rounded-2xl border p-6 shadow-sm ${card}`}><div className="mb-6 flex items-center gap-2">{['Job Information', 'Live Preview', 'Publish'].map((label, index) => <div key={label} className={`flex flex-1 items-center gap-2 text-xs font-bold ${wizard === index + 1 ? 'text-blue-600' : 'text-slate-400'}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full ${wizard >= index + 1 ? 'bg-blue-600 text-white' : 'bg-slate-100'}`}>{index + 1}</span>{label}</div>)}</div>{wizard === 1 && <div className="grid gap-5 md:grid-cols-2"><Field label="Job Title"><input className={inputClass(dark)} value={job.title} onChange={(e) => setJob({ ...job, title: e.target.value })} placeholder="Senior React Developer" /></Field><Field label="Department"><input className={inputClass(dark)} value={job.department} onChange={(e) => setJob({ ...job, department: e.target.value })} /></Field><Field label="Job Type"><select className={inputClass(dark)} value={job.job_type} onChange={(e) => setJob({ ...job, job_type: e.target.value })}><option value="full-time">Full-time</option><option value="part-time">Part-time</option><option value="contract">Contract</option><option value="internship">Internship</option></select></Field><Field label="Work Model"><select className={inputClass(dark)} value={job.work_mode} onChange={(e) => setJob({ ...job, work_mode: e.target.value })}><option value="remote">Remote</option><option value="hybrid">Hybrid</option><option value="on-site">On-site</option></select></Field><Field label="Location"><input className={inputClass(dark)} value={job.location} onChange={(e) => setJob({ ...job, location: e.target.value })} /></Field><Field label="Currency"><select className={inputClass(dark)} value={job.currency} onChange={(e) => setJob({ ...job, currency: e.target.value })}><option>ETB</option><option>USD</option></select></Field><Field label="Minimum Salary"><input type="number" className={inputClass(dark)} value={job.salary_min} onChange={(e) => setJob({ ...job, salary_min: e.target.value })} /></Field><Field label="Maximum Salary"><input type="number" className={inputClass(dark)} value={job.salary_max} onChange={(e) => setJob({ ...job, salary_max: e.target.value })} /></Field><Field label="Deadline"><input type="date" className={inputClass(dark)} value={job.application_deadline} onChange={(e) => setJob({ ...job, application_deadline: e.target.value })} /></Field><Field label="Required Skills (comma separated)"><input className={inputClass(dark)} value={job.required_skills} onChange={(e) => setJob({ ...job, required_skills: e.target.value })} placeholder="React, Node.js, SQL" /></Field><div className="md:col-span-2"><Field label="Description"><textarea rows="8" className={inputClass(dark)} value={job.description} onChange={(e) => setJob({ ...job, description: e.target.value })} /><button type="button" onClick={enhanceJob} className="mt-3 flex items-center gap-2 rounded-xl bg-violet-50 px-4 py-2 text-xs font-bold text-violet-700"><Sparkles className="h-4 w-4" /> AI Enhance Job Description</button></Field></div></div>}{wizard === 2 && <div className="rounded-2xl border border-slate-200 p-6"><p className="text-xs font-bold uppercase tracking-widest text-blue-600">Candidate preview</p><h3 className="mt-3 text-2xl font-black">{job.title || 'Your new job title'}</h3><p className="mt-2 text-sm text-slate-500">{companyName} Â· {job.location || 'Location'} Â· {job.work_mode}</p><div className="mt-6 whitespace-pre-line text-sm leading-7 text-slate-600">{job.description || 'Your enhanced job description will appear here.'}</div></div>}{wizard === 3 && <div className="rounded-2xl bg-blue-50 p-8 text-center"><CheckCircle2 className="mx-auto h-12 w-12 text-blue-600" /><h3 className="mt-4 text-2xl font-black">Ready to publish?</h3><p className="mx-auto mt-2 max-w-md text-sm text-slate-600">Save this role as a draft, schedule it later, or publish it now for candidates to discover.</p><div className="mt-6 flex flex-wrap justify-center gap-3"><button onClick={() => saveJob('draft')} className="rounded-xl bg-white px-4 py-3 text-sm font-bold text-slate-700">Save as Draft</button><button onClick={() => { saveJob('draft'); notify('Job scheduled for publishing'); }} className="rounded-xl bg-amber-500 px-4 py-3 text-sm font-bold text-white">Schedule Post</button><button onClick={() => saveJob('published')} className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-bold text-white">Publish Job Now</button></div></div>}<div className="mt-8 flex justify-between"><button disabled={wizard === 1} onClick={() => setWizard((value) => value - 1)} className="rounded-xl border px-4 py-2 text-sm font-bold disabled:opacity-30">Back</button>{wizard < 3 && <button onClick={() => setWizard((value) => value + 1)} className="flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2 text-sm font-bold text-white">Continue <ChevronRight className="h-4 w-4" /></button>}</div></div><div className={`rounded-2xl border p-6 shadow-sm ${card}`}><p className="text-xs font-bold uppercase tracking-widest text-violet-600">AI assistant</p><h3 className="mt-2 text-xl font-black">Build a better role</h3><p className="mt-2 text-sm leading-6 text-slate-500">Use structured responsibilities and screening questions to improve candidate signal.</p><div className="mt-6 space-y-3 text-sm text-slate-600"><p>âœ“ Skills-based screening</p><p>âœ“ Candidate-facing preview</p><p>âœ“ ETB and USD salary support</p><p>âœ“ Draft, schedule, or publish</p></div></div></div>}

      {active === 'jobs' && <div className="grid gap-5 lg:grid-cols-2">{jobs.map((item) => <div className={`rounded-2xl border p-5 shadow-sm ${card}`} key={item.id}><div className="flex items-start justify-between"><div><h3 className="text-lg font-black">{item.title}</h3><p className="mt-1 text-sm text-slate-500">{item.department || item.category} Â· {item.location}</p></div><span className={`rounded-full px-3 py-1 text-xs font-bold capitalize ${item.status === 'published' ? 'bg-emerald-50 text-emerald-700' : item.status === 'paused' ? 'bg-amber-50 text-amber-700' : 'bg-slate-100 text-slate-600'}`}>{item.status}</span></div><div className="mt-5 flex gap-6 border-y border-slate-100 py-4 text-sm"><span><b>{item.applicantsCount || 0}</b> Applicants</span><span><b>{item.shortlisted || 0}</b> Shortlisted</span></div><div className="flex flex-wrap gap-2"><button onClick={() => editJob(item)} className="inline-flex items-center gap-1 rounded-xl bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700">Edit</button><button onClick={() => { setSearch(item.title); setActive('applications'); }} className="rounded-xl bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700">View Applicants</button><button onClick={() => toggleJob(item)} className="rounded-xl bg-slate-100 px-3 py-2 text-xs font-bold">{item.status === 'published' ? 'Pause' : 'Activate'}</button><button onClick={() => deleteJob(item.id)} className="rounded-xl bg-red-50 p-2 text-red-600"><Trash2 className="h-4 w-4" /></button></div></div>)}</div>}

      {['applications', 'matching', 'shortlist', 'hired'].includes(active) && <div className="space-y-5"><div className={`flex flex-wrap gap-3 rounded-2xl border p-4 ${card}`}><div className="relative min-w-56 flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input className={`${inputClass(dark)} pl-10`} placeholder="Search candidates or roles" value={search} onChange={(e) => setSearch(e.target.value)} /></div><select className={inputClass(dark)} value={status} onChange={(e) => setStatus(e.target.value)}><option value="all">All statuses</option><option value="applied">Applied</option><option value="under-review">Under Review</option><option value="shortlisted">Shortlisted</option><option value="interview-scheduled">Interview</option><option value="hired">Hired</option><option value="rejected">Rejected</option></select><select className={inputClass(dark)} value={minScore} onChange={(e) => setMinScore(Number(e.target.value))}><option value="0">Any AI score</option><option value="65">65%+ score</option><option value="80">80%+ score</option><option value="90">90%+ score</option></select></div>{active === 'matching' && <div className="grid gap-4 md:grid-cols-4">{[['Hard Skills Overlap', 40], ['Experience Relevance', 30], ['Education & Certs', 15], ['Work Model & Location', 15]].map(([label, weight]) => <div className={`rounded-2xl border p-4 ${card}`} key={label}><p className="text-xs text-slate-500">{label}</p><p className="mt-2 text-2xl font-black">{weight}%</p><div className="mt-3 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600" style={{ width: `${weight * 2.5}%` }} /></div></div>)}</div>}{active === 'shortlist' && <div className="rounded-2xl border border-blue-100 bg-blue-50 p-5"><h3 className="font-black text-blue-900">Shortlist ready for action</h3><p className="mt-1 text-sm text-blue-700">Move high-signal candidates into interviews or export your shortlist.</p></div>}{renderApplications(active === 'shortlist' ? filtered.filter((item) => item.status === 'shortlisted') : active === 'hired' ? filtered.filter((item) => item.status === 'hired') : filtered)}</div>}

      {active === 'interviews' && <div className="grid gap-5 lg:grid-cols-2">{interviews.length ? interviews.map((item, index) => <div className={`rounded-2xl border p-5 ${card}`} key={item.id || index}><div className="flex items-center gap-3"><CalendarDays className="h-8 w-8 text-violet-600" /><div><h3 className="font-black">{item.candidate_name || item.candidateName}</h3><p className="text-sm text-slate-500">{item.job_title} Â· {new Date(item.scheduled_at).toLocaleString()}</p></div></div><button onClick={() => notify('Scorecard opened')} className="mt-5 rounded-xl bg-violet-50 px-3 py-2 text-xs font-bold text-violet-700">Open Scorecard</button></div>) : <div className={`rounded-2xl border p-10 text-center ${card}`}><CalendarDays className="mx-auto h-10 w-10 text-slate-300" /><h3 className="mt-3 font-black">No interviews scheduled</h3><p className="mt-1 text-sm text-slate-500">Schedule an interview directly from an application.</p></div>}</div>}
      {active === 'summary' && <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><TopCandidatesList jobId={matchingJobId} onSelectCandidate={setAiCandidate} onShortlist={(candidate) => updateApplication(candidate.applicationId, 'shortlisted')} onSchedule={(candidate) => { setSelected({ ...candidate, id: candidate.applicationId, candidateId: candidate.candidateId }); setShowSchedule(true); }} /></div>}
      {active === 'reviews' && <div className="space-y-8"><CompanyReviews companyId={company.id || 1} employerView /><CompanyQA companyId={company.id || 1} employerView /></div>}
    </div></main></div><Toast toast={toast} onClose={() => setToast('')} />
    {selected && <div className="fixed inset-0 z-[60] flex items-center justify-center bg-slate-950/60 p-4"><div className={`max-h-[90vh] w-full max-w-2xl overflow-y-auto rounded-3xl p-6 shadow-2xl ${card}`}><div className="flex items-start justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-blue-600">Candidate CV Viewer</p><h2 className="mt-1 text-2xl font-black">{selected.name}</h2><p className="text-sm text-slate-500">{selected.email} Â· {selected.jobTitle}</p></div><button onClick={() => setSelected(null)}><X /></button></div><div className="mt-6 grid gap-4 sm:grid-cols-[auto_1fr]"><ScoreRing score={selected.matchScore} size={90} /><div><h3 className="font-black">AI Match Breakdown</h3><p className="mt-1 text-sm leading-6 text-slate-500">Strong match based on skills and experience. Review the skill gaps before the final decision.</p><div className="mt-3 flex flex-wrap gap-2">{selected.skills?.map((skill) => <span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700" key={skill}>{skill}</span>)}</div></div></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-400">Work history</p><p className="mt-2 font-bold">{selected.experience} relevant experience</p><p className="mt-1 text-sm text-slate-500">Product delivery and measurable outcomes.</p></div><div className="rounded-xl bg-slate-50 p-4"><p className="text-xs font-bold text-slate-400">Education</p><p className="mt-2 font-bold">{selected.education}</p><p className="mt-1 text-sm text-slate-500">CV parsing completed successfully.</p></div></div><div className="mt-6 flex flex-wrap gap-2"><button onClick={() => updateApplication(selected.id, 'shortlisted')} className="rounded-xl bg-emerald-600 px-4 py-3 text-sm font-bold text-white">Shortlist</button><button onClick={() => setShowSchedule(true)} className="rounded-xl bg-violet-600 px-4 py-3 text-sm font-bold text-white">Schedule Interview</button><button onClick={() => hire(selected)} className="rounded-xl bg-blue-600 px-4 py-3 text-sm font-bold text-white">Mark Hired</button><a href={selected.resumeUrl || '#'} className="rounded-xl bg-slate-100 px-4 py-3 text-sm font-bold">View PDF</a></div></div></div>}
    {showSchedule && selected && <div className="fixed inset-0 z-[80] flex items-center justify-center bg-slate-950/60 p-4"><form onSubmit={scheduleInterview} className={`w-full max-w-lg space-y-5 rounded-3xl p-6 shadow-2xl ${card}`}><div className="flex items-center justify-between"><h2 className="text-xl font-black">Schedule Interview</h2><button type="button" onClick={() => setShowSchedule(false)}><X /></button></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Date"><input required type="date" className={inputClass(dark)} value={schedule.date} onChange={(e) => setSchedule({ ...schedule, date: e.target.value })} /></Field><Field label="Time"><input required type="time" className={inputClass(dark)} value={schedule.time} onChange={(e) => setSchedule({ ...schedule, time: e.target.value })} /></Field><Field label="Meeting Type"><select className={inputClass(dark)} value={schedule.type} onChange={(e) => setSchedule({ ...schedule, type: e.target.value })}><option value="video">Video</option><option value="in-person">In-person</option></select></Field><Field label="Meeting Link"><input className={inputClass(dark)} value={schedule.link} onChange={(e) => setSchedule({ ...schedule, link: e.target.value })} placeholder="https://meet.google.com/..." /></Field></div><Field label="Agenda Notes"><textarea rows="4" className={inputClass(dark)} value={schedule.notes} onChange={(e) => setSchedule({ ...schedule, notes: e.target.value })} /></Field><button className="w-full rounded-xl bg-blue-600 py-3 text-sm font-bold text-white">Confirm & Schedule</button></form></div>}
  </div>;
}

