import { ArrowLeft, BadgeCheck, ExternalLink } from 'lucide-react';
import { useLocation, useNavigate, useParams } from 'react-router-dom';
export default function CertificatePreview() { const { id } = useParams(); const { state } = useLocation(); const navigate = useNavigate(); const certificate = state?.certificate || {}; const url = certificate.credentialUrl || certificate.credentialFile; return <main className="information-page min-h-[70vh] bg-slate-50 px-4 py-8 sm:px-6"><div className="mx-auto max-w-2xl"><button type="button" onClick={() => navigate('/profile')} className="mb-5 inline-flex min-h-11 items-center gap-2 text-sm font-bold text-[var(--brand-deep)]"><ArrowLeft className="h-4 w-4" /> Back to profile</button><section className="rounded-3xl border border-slate-200 bg-white p-8 text-center shadow-sm"><BadgeCheck className="mx-auto h-12 w-12 text-[var(--brand-primary)]" /><p className="mt-4 text-xs font-bold uppercase tracking-wider text-[var(--brand-deep)]">Certificate {id}</p><h1 className="mt-2 text-2xl font-black text-slate-900">{certificate.title || 'Certificate Preview'}</h1><p className="mt-2 text-slate-500">{certificate.issuer || 'Issuing organization'}</p>{url ? <a href={url} target="_blank" rel="noreferrer" className="mt-6 inline-flex min-h-11 items-center gap-2 rounded-xl bg-[var(--brand-primary)] px-5 py-3 text-sm font-bold text-white">Open credential <ExternalLink className="h-4 w-4" /></a> : <p className="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-500">No credential document or URL has been uploaded yet.</p>}</section></div></main>; }
