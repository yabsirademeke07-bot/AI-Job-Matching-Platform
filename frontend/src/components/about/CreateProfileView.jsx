import { useState } from "react";
import { Building2, CheckCircle2, FileText, ShieldCheck, UserRound } from "lucide-react";

const roles = [
  ["seeker", "Job Seeker", "Find jobs with AI-powered recommendations", UserRound],
  ["employer", "Employer", "Find the best candidates for your company", Building2],
  ["admin", "Admin", "Manage and monitor the entire platform", ShieldCheck],
];

const inputClass = "mt-1 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm text-slate-700 outline-none focus:ring-2 focus:ring-blue-500";

export default function CreateProfileView() {
  const [role, setRole] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [skills, setSkills] = useState("");
  const submit = (event) => { event.preventDefault(); setSubmitted(true); };

  return <section className="space-y-6"><header className="rounded-3xl border border-blue-100 bg-white p-6 shadow-sm sm:p-8"><p className="text-xs font-bold uppercase tracking-[0.16em] text-blue-600">Create your profile</p><h1 className="mt-3 text-3xl font-black tracking-tight text-slate-900 sm:text-4xl">Choose your account type</h1><p className="mt-3 text-base leading-7 text-slate-600">Create the profile that matches your role in the AI-powered hiring journey.</p></header><div className="grid gap-4 md:grid-cols-3">{roles.map(([id, title, description, Icon]) => <button key={id} type="button" onClick={() => { setRole(id); setSubmitted(false); }} className={`rounded-2xl border p-5 text-left shadow-sm transition ${role === id ? "border-blue-500 bg-blue-50 ring-2 ring-blue-100" : "border-slate-200 bg-white hover:border-blue-300"}`}><Icon className={`h-7 w-7 ${role === id ? "text-blue-700" : "text-slate-500"}`} /><h2 className="mt-4 text-lg font-black text-slate-900">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{description}</p></button>)}</div>{!role && <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-8 text-center text-sm text-slate-500">Select Job Seeker, Employer, or Admin to continue.</div>}{role === "admin" && <div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-800"><strong>Admin access is restricted.</strong> Admin profiles should only be created by the System Owner or an existing Super Admin.</div>}{role && role !== "admin" && <form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><h2 className="text-2xl font-black text-slate-900">{role === "seeker" ? "Job Seeker Profile" : "Employer Company Profile"}</h2><div className="mt-6 grid gap-4 md:grid-cols-2"><label className="text-sm font-bold text-slate-700">{role === "seeker" ? "Full Name" : "Company Name"}<input required className={inputClass} placeholder={role === "seeker" ? "Aman Bekele" : "Company name"} /></label><label className="text-sm font-bold text-slate-700">Email<input required type="email" className={inputClass} placeholder="name@example.com" /></label><label className="text-sm font-bold text-slate-700">Phone Number<input className={inputClass} placeholder="+251 9..." /></label><label className="text-sm font-bold text-slate-700">Location<input className={inputClass} placeholder="Addis Ababa" /></label>{role === "seeker" ? <><label className="text-sm font-bold text-slate-700">Professional Title<input className={inputClass} placeholder="Frontend Developer" /></label><label className="text-sm font-bold text-slate-700">Career Level<select className={inputClass}><option>Junior</option><option>Mid-level</option><option>Senior</option></select></label><label className="text-sm font-bold text-slate-700 md:col-span-2">Technical and Soft Skills<textarea value={skills} onChange={(event) => setSkills(event.target.value)} className={inputClass} rows="3" placeholder="React, Node.js, Communication" /></label><label className="flex items-center gap-3 rounded-xl border border-blue-100 bg-blue-50 p-4 text-sm font-bold text-blue-700 md:col-span-2"><FileText className="h-5 w-5" /> Upload Your CV<input type="file" accept=".pdf,.doc,.docx" className="ml-auto max-w-[180px] text-xs" /></label><label className="text-sm font-bold text-slate-700">Desired Job Title<input className={inputClass} placeholder="Full Stack Developer" /></label><label className="text-sm font-bold text-slate-700">Preferred Job Type<select className={inputClass}><option>Full-time</option><option>Part-time</option><option>Internship</option><option>Remote</option></select></label></> : <><label className="text-sm font-bold text-slate-700">Industry<input className={inputClass} placeholder="Technology" /></label><label className="text-sm font-bold text-slate-700">Company Size<select className={inputClass}><option>1–10</option><option>11–50</option><option>51–200</option><option>201+</option></select></label><label className="text-sm font-bold text-slate-700 md:col-span-2">Company Description<textarea required className={inputClass} rows="4" placeholder="Tell us about your company..." /></label><label className="text-sm font-bold text-slate-700">Website<input className={inputClass} placeholder="https://example.com" /></label><label className="text-sm font-bold text-slate-700">Hiring Needs<input className={inputClass} placeholder="Frontend and backend engineers" /></label></>}</div><button type="submit" className="mt-6 rounded-xl bg-blue-600 px-5 py-3 text-sm font-bold text-white hover:bg-blue-700">{role === "seeker" ? "Create My Profile" : "Create Company Profile"}</button>{submitted && <p className="mt-4 flex items-center gap-2 rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-700"><CheckCircle2 className="h-4 w-4" /> Profile information is ready to be saved.</p>}</form>}</section>;
}
