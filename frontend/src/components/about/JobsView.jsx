import { useState } from "react";
import { useNavigate } from "react-router-dom";
import { useAuth } from "../../context/AuthContext";
import { beginApplication } from "../../utils/applicationFlow";
import { Bookmark, BriefcaseBusiness, Building2, Clock3, MapPin, Search, Sparkles, Target } from "lucide-react";

const jobs = [
  ["Frontend Developer", "Tech Solutions", "Addis Ababa", "Full-time", "Software Development", 96, ["React.js", "JavaScript", "CSS"]],
  ["Full Stack Developer", "ABC Company", "Remote", "Full-time", "Software Development", 93, ["React.js", "Node.js", "MySQL"]],
  ["Backend Developer", "Digital Works", "Addis Ababa", "Full-time", "Software Development", 89, ["Node.js", "Express.js", "REST APIs"]],
  ["UI/UX Designer", "Creative Hub", "Hybrid", "Contract", "Design", 85, ["Figma", "Research", "Prototyping"]],
  ["Data Analyst", "Insight Africa", "Remote", "Full-time", "Data & Analytics", 78, ["SQL", "Python", "Analytics"]],
  ["Marketing Specialist", "Growth Lab", "Addis Ababa", "Part-time", "Marketing", 68, ["Content", "SEO", "Campaigns"]],
].map(([title, company, location, type, category, match, skills]) => ({ title, company, location, type, category, match, skills }));
const categories = ["All Categories", "Software Development", "Design", "Data & Analytics", "Marketing", "Finance", "Healthcare", "Education"];

export default function JobsView() {
  const navigate = useNavigate();
  const { user, isAuthenticated } = useAuth();
  const [query, setQuery] = useState("");
  const [location, setLocation] = useState("All locations");
  const [type, setType] = useState("All job types");
  const [category, setCategory] = useState("All Categories");
  const [minimumMatch, setMinimumMatch] = useState(0);
  const [saved, setSaved] = useState(() => JSON.parse(localStorage.getItem("aboutSavedJobs") || "[]"));
  const toggleSaved = (id) => setSaved((current) => { const next = current.includes(id) ? current.filter((item) => item !== id) : [...current, id]; localStorage.setItem("aboutSavedJobs", JSON.stringify(next)); return next; });
  const filtered = jobs.filter((job) => { const text = query.toLowerCase(); return (!text || [job.title, job.company, ...job.skills].some((value) => value.toLowerCase().includes(text))) && (location === "All locations" || job.location === location) && (type === "All job types" || job.type === type) && (category === "All Categories" || job.category === category) && job.match >= minimumMatch; });
  const apply = (job) => beginApplication(job.title, job, navigate, { isAuthenticated, role: user?.role, sourcePage: "/about/jobs", returnPath: "/about/jobs" });
  return <section className="space-y-6"><header className="rounded-3xl border border-blue-100 bg-white p-6 shadow-sm sm:p-8"><span className="inline-flex items-center gap-2 rounded-full border border-blue-200 bg-blue-50 px-3 py-1.5 text-xs font-bold uppercase tracking-[0.16em] text-blue-700"><BriefcaseBusiness className="h-3.5 w-3.5" /> Explore jobs</span><h1 className="mt-5 text-3xl font-black tracking-tight text-slate-900 sm:text-4xl">Find opportunities that match your skills and career goals.</h1><p className="mt-3 max-w-3xl text-base leading-7 text-slate-600">Search smarter with AI recommendations, transparent match scores, and filters built around the way you work.</p></header><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="relative"><Search className="absolute left-3 top-1/2 h-4 w-4 -translate-y-1/2 text-slate-400" /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search by job title, skill, or company..." className="w-full rounded-xl border border-slate-200 bg-slate-50 py-3 pl-10 pr-4 text-sm outline-none focus:ring-2 focus:ring-blue-500" /></div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><select value={location} onChange={(event) => setLocation(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option>All locations</option><option>Addis Ababa</option><option>Remote</option><option>Hybrid</option></select><select value={type} onChange={(event) => setType(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option>All job types</option><option>Full-time</option><option>Part-time</option><option>Contract</option></select><select value={category} onChange={(event) => setCategory(event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm">{categories.map((item) => <option key={item}>{item}</option>)}</select><select value={minimumMatch} onChange={(event) => setMinimumMatch(Number(event.target.value))} className="rounded-xl border border-slate-200 px-3 py-2.5 text-sm"><option value="0">All match scores</option><option value="90">90–100% Best Match</option><option value="75">75–89% Good Match</option><option value="50">50–74% Moderate Match</option></select></div></div><div className="flex items-center gap-2"><Sparkles className="h-5 w-5 text-violet-600" /><h2 className="text-xl font-black text-slate-900">Top AI Matches For You</h2></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{filtered.filter((job) => job.match >= 85).slice(0, 3).map((job) => <JobCard key={`top-${job.title}`} job={job} saved={saved.includes(job.title)} onSave={toggleSaved} onApply={apply} onDetails={() => navigate(`/jobs/${job.title}`)} />)}</div><div className="flex items-center justify-between"><h2 className="text-xl font-black text-slate-900">All Available Jobs <span className="text-sm font-semibold text-slate-400">({filtered.length})</span></h2></div><div className="grid gap-4 md:grid-cols-2">{filtered.map((job) => <JobCard key={job.title} job={job} saved={saved.includes(job.title)} onSave={toggleSaved} onApply={apply} onDetails={() => navigate(`/jobs/${job.title}`)} />)}</div></section>;
}

function JobCard({ job, saved, onSave, onApply, onDetails }) {
  return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm hover:shadow-md"><div className="flex items-start justify-between gap-3"><div><h3 className="text-lg font-black text-slate-900">{job.title}</h3><p className="mt-1 flex items-center gap-1 text-sm font-semibold text-slate-500"><Building2 className="h-3.5 w-3.5" /> {job.company}</p></div><button type="button" onClick={() => onSave(job.title)} className={saved ? "text-rose-500" : "text-slate-400 hover:text-rose-500"} aria-label={`Save ${job.title}`}><Bookmark className="h-5 w-5" fill={saved ? "currentColor" : "none"} /></button></div><div className="mt-4 flex flex-wrap gap-3 text-xs text-slate-500"><span className="inline-flex items-center gap-1"><MapPin className="h-3.5 w-3.5" />{job.location}</span><span className="inline-flex items-center gap-1"><Clock3 className="h-3.5 w-3.5" />{job.type}</span></div><div className="mt-4 flex flex-wrap gap-2">{job.skills.map((skill) => <span key={skill} className="rounded-lg bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-600">{skill}</span>)}</div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-black text-emerald-700"><Target className="h-3.5 w-3.5" /> AI Match: {job.match}%</span><div className="flex gap-2"><button type="button" onClick={onDetails} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold text-slate-700">View Details</button><button type="button" onClick={() => onApply(job)} className="rounded-xl bg-blue-600 px-3 py-2 text-xs font-bold text-white">Apply Now</button></div></div></article>;
}
