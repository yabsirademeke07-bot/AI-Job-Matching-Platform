import { ArrowRight } from 'lucide-react';
import RecommendedJobCard from './RecommendedJobCard';
export default function RecommendedJobs({ jobs = [], onViewDetails, onViewAll, isLoading, error, onRetry }) { return <section aria-labelledby="recommended-jobs-heading"><div className="mb-4 flex items-center justify-between"><h2 id="recommended-jobs-heading" className="text-xl font-black text-slate-900">Recommended Jobs</h2><button type="button" onClick={onViewAll} className="inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]">View All <ArrowRight className="h-4 w-4" /></button></div>{isLoading && <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[1, 2, 3].map((id) => <div key={id} className="h-48 animate-pulse rounded-2xl bg-slate-200" />)}</div>}{error && <div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}<button type="button" onClick={onRetry} className="ml-3 font-bold underline">Refresh</button></div>}{!isLoading && !error && jobs.length === 0 && <p className="rounded-2xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">No recommended jobs available right now.</p>}{!isLoading && !error && <div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{jobs.map((job) => <RecommendedJobCard key={job.id} job={job} onViewDetails={onViewDetails} />)}</div>}</section>; }
