import { ArrowRight } from 'lucide-react';
import ApplicationCard from './ApplicationCard';
export default function RecentApplications({ applications = [], onViewDetails, onViewAll, isLoading, error, onRetry }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-labelledby="recent-applications-heading"><div className="mb-4 flex items-center justify-between"><h2 id="recent-applications-heading" className="text-lg font-black text-slate-900">Recent Applications</h2><button type="button" onClick={onViewAll} className="inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]">View All <ArrowRight className="h-4 w-4" /></button></div>{isLoading && <div className="space-y-3">{[1, 2, 3].map((id) => <div key={id} className="h-16 animate-pulse rounded-xl bg-slate-100" />)}</div>}{error && <div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}<button type="button" onClick={onRetry} className="ml-3 font-bold underline">Refresh</button></div>}{!isLoading && !error && applications.length === 0 && <p className="rounded-xl bg-slate-50 p-6 text-center text-sm text-slate-500">No applications yet.</p>}{!isLoading && !error && applications.map((application) => <ApplicationCard key={application.id} application={application} onViewDetails={onViewDetails} />)}</section>; }
