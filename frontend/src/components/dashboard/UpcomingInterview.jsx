import { CalendarClock } from 'lucide-react';
import InterviewCard from './InterviewCard';
export default function UpcomingInterview({ interview, onViewDetails, onJoin, isLoading, error, onRetry }) { if (isLoading) return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="h-48 animate-pulse rounded-xl bg-slate-100" /></section>; if (error) return <section className="rounded-2xl border border-red-200 bg-red-50 p-5 text-sm text-red-700">{error}<button type="button" onClick={onRetry} className="ml-3 font-bold underline">Refresh</button></section>; if (!interview) return null; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-labelledby="upcoming-interview-heading"><h2 id="upcoming-interview-heading" className="mb-4 flex items-center gap-2 text-lg font-black text-slate-900"><CalendarClock className="h-5 w-5 text-[var(--brand-primary)]" /> Upcoming Interview</h2><InterviewCard interview={interview} onViewDetails={onViewDetails} onJoin={onJoin} /></section>; }
