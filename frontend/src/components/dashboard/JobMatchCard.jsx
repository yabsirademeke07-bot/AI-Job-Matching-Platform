import { Bookmark, BookmarkCheck, Lightbulb, MapPin } from 'lucide-react';
export default function JobMatchCard({ job, saved, onSave, onViewDetails }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h3 className="font-black text-slate-900">{job.title}</h3><p className="mt-1 text-sm font-semibold text-slate-500">{job.company}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-black text-emerald-700">{job.matchScore}% match</span></div><p className="mt-3 flex items-center gap-1 text-xs text-slate-500"><MapPin className="h-3.5 w-3.5" /> {job.location} · {job.salary}</p><div className="mt-4 rounded-xl bg-[var(--brand-soft)] p-3 text-xs text-slate-600"><p className="flex items-center gap-1 font-bold text-[var(--brand-deep)]"><Lightbulb className="h-3.5 w-3.5" /> Why this matches</p><ul className="mt-2 list-disc space-y-1 pl-4">{(job.reasons || job.whyMatch || []).map((reason) => <li key={reason}>{reason}</li>)}</ul></div><div className="mt-4 flex items-center justify-between"><button type="button" onClick={() => onSave(job.id)} className="inline-flex items-center gap-1 text-sm font-bold text-slate-600">{saved ? <BookmarkCheck className="h-4 w-4 text-[var(--brand-deep)]" /> : <Bookmark className="h-4 w-4" />}{saved ? 'Saved' : 'Save Job'}</button><button type="button" onClick={() => onViewDetails(job.id)} className="rounded-xl bg-[var(--brand-primary)] px-4 py-2 text-sm font-bold text-white hover:bg-[var(--brand-primary-hover)]">View Details</button></div></article>; }
