import { ArrowRight, MapPin } from 'lucide-react';
export default function RecommendedJobCard({ job, onViewDetails }) { return <article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-[var(--brand-soft)] font-black text-[var(--brand-deep)]">{job.company?.charAt(0) || 'J'}</div><h3 className="mt-4 font-black text-slate-900">{job.title}</h3><p className="mt-1 text-sm font-semibold text-slate-500">{job.company}</p><p className="mt-4 text-xs text-slate-500"><MapPin className="mr-1 inline h-3.5 w-3.5" />{job.location}</p><p className="mt-2 text-sm font-bold text-slate-700">{job.salary}</p><div className="mt-4 flex flex-wrap gap-1.5">{(job.tags || []).map((tag) => <span key={tag} className="rounded-lg bg-slate-100 px-2 py-1 text-xs font-semibold text-slate-600">{tag}</span>)}</div><button type="button" onClick={() => onViewDetails(job.id)} className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]">View Details <ArrowRight className="h-4 w-4" /></button></article>; }
