import { ArrowRight, CheckCircle2, Sparkles } from 'lucide-react';

export default function WelcomeCard({ user, onCompleteProfile }) {
  const name = user?.name || user?.full_name || 'User';
  const initials = name.split(' ').map((part) => part[0]).join('').slice(0, 2).toUpperCase();
  const completion = Number(user?.profileCompletion ?? user?.profile_completion ?? 0);
  return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8" aria-labelledby="welcome-heading"><div className="flex flex-col gap-6 md:flex-row md:items-center md:justify-between"><div className="flex items-center gap-4"><div className="flex h-16 w-16 items-center justify-center rounded-2xl bg-[var(--brand-primary)] text-xl font-black text-white">{initials || 'U'}</div><div><div className="mb-2 inline-flex items-center gap-2 rounded-full bg-[var(--brand-primary)] px-3 py-1 text-xs font-bold text-white"><Sparkles className="h-3.5 w-3.5" /> AI Career Dashboard</div><h1 id="welcome-heading" className="text-2xl font-black text-slate-900">Good {new Date().getHours() < 12 ? 'Morning' : new Date().getHours() < 18 ? 'Afternoon' : 'Evening'}, {name} <span className="text-[var(--brand-primary)]" aria-label="waving hand">👋</span></h1><p className="mt-1 text-sm text-slate-500">Welcome back! Continue your job search and find opportunities that match your skills.</p></div></div><div className="min-w-64 rounded-2xl bg-[var(--brand-soft)] p-4"><div className="flex items-center justify-between text-sm font-bold text-slate-700"><span>Profile completion</span><span>{completion}% Complete</span></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white"><div className="h-full rounded-full bg-[var(--brand-primary)]" style={{ width: `${Math.min(100, completion)}%` }} /></div><button type="button" onClick={onCompleteProfile} className="dashboard-action-button mt-4 inline-flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold text-white">Complete Profile <ArrowRight className="h-4 w-4" /></button></div></div><p className="mt-5 flex items-center gap-1 text-xs font-semibold text-slate-500"><CheckCircle2 className="h-4 w-4 text-emerald-500" /> Your profile helps our AI find a better fit.</p></section>;
}
