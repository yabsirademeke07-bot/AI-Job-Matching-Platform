import { ArrowRight, Target } from 'lucide-react';
import JobMatchCard from './JobMatchCard';
export default function AIJobMatches({ jobs = [], savedJobs, onSave, onViewDetails, onViewAll, isLoading, error, onRetry }) { return <section aria-labelledby="ai-job-matches-heading"><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--brand-deep)]">Personalized for you</p><h2 id="ai-job-matches-heading" className="mt-1 text-xl font-black text-slate-900">AI Job Matches</h2></div><button type="button" onClick={onViewAll} className="inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]">View All <ArrowRight className="h-4 w-4" /></button></div>{isLoading && <div className="grid gap-4 md:grid-cols-2">{[1, 2].map((id) => <div key={id} className="h-56 animate-pulse rounded-2xl bg-slate-200" />)}</div>}{error && <div className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}<button type="button" onClick={onRetry} className="ml-3 font-bold underline">Refresh</button></div>}{!isLoading && !error && jobs.length === 0 && <div className="rounded-2xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500"><Target className="mx-auto mb-2 h-6 w-6" />No job matches found yet.</div>}<div className="grid gap-4 md:grid-cols-2">{!isLoading && !error && jobs.map((job) => <JobMatchCard key={job.id} job={job} saved={savedJobs.includes(job.id)} onSave={onSave} onViewDetails={onViewDetails} />)}</div></section>; }
