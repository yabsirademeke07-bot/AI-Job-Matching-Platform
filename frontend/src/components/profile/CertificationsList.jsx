import { BadgeCheck, Plus } from 'lucide-react';
export default function CertificationsList({ items = [], onAdd }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-labelledby="certifications-heading"><div className="flex items-center justify-between"><h2 id="certifications-heading" className="flex items-center gap-2 text-xl font-black text-slate-900"><BadgeCheck className="h-5 w-5 text-[var(--brand-deep)]" /> Certifications</h2><button type="button" onClick={onAdd} className="inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]"><Plus className="h-4 w-4" /> Add</button></div>{items.length === 0 ? <p className="mt-5 rounded-xl bg-slate-50 p-5 text-sm text-slate-500">Add certifications and credentials to show verified expertise.</p> : <div className="mt-5 space-y-3">{items.map((item) => <article key={item.id} className="rounded-xl border border-slate-100 p-4"><h3 className="font-bold text-slate-900">{item.title || 'Certification title'}</h3><p className="mt-1 text-sm text-slate-500">{item.issuer || 'Issuing organization'}</p><p className="mt-2 text-xs text-slate-400">Issued {item.issueDate || 'date'}{item.expirationDate ? ` · Expires ${item.expirationDate}` : ''}</p>{item.credentialUrl && <a href={item.credentialUrl} target="_blank" rel="noreferrer" className="mt-2 inline-block text-sm font-bold text-[var(--brand-deep)] hover:underline">View credential</a>}</article>)}</div>}</section>; }
