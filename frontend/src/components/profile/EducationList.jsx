import { GraduationCap, Plus } from 'lucide-react';
export default function EducationList({ items = [], onAdd }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-labelledby="education-heading"><div className="flex items-center justify-between"><h2 id="education-heading" className="flex items-center gap-2 text-xl font-black text-slate-900"><GraduationCap className="h-5 w-5 text-[var(--brand-deep)]" /> Education</h2><button type="button" onClick={onAdd} className="inline-flex items-center gap-1 text-sm font-bold text-[var(--brand-deep)]"><Plus className="h-4 w-4" /> Add</button></div>{items.length === 0 ? <p className="mt-5 rounded-xl bg-slate-50 p-5 text-sm text-slate-500">Add your education to help employers understand your background.</p> : <div className="mt-5 space-y-3">{items.map((item) => <article key={item.id} className="rounded-xl border border-slate-100 p-4"><h3 className="font-bold text-slate-900">{item.degree || 'Degree or qualification'}</h3><p className="mt-1 text-sm font-semibold text-slate-500">{item.institution || item.university || 'Institution'}{item.field ? ` · ${item.field}` : ''}</p><p className="mt-2 text-xs text-slate-400">{item.startDate || 'Start date'} – {item.endDate || 'Present'}{item.gpa ? ` · GPA ${item.gpa}` : ''}</p>{item.description && <p className="mt-2 text-sm text-slate-600">{item.description}</p>}</article>)}</div>}</section>; }
