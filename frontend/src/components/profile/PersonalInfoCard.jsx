import { Camera, Mail, MapPin, Phone } from 'lucide-react';

export default function PersonalInfoCard({ profile, onEdit, onAvatarChange }) {
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm" aria-labelledby="personal-info-heading"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--brand-deep)]">Candidate profile</p><h2 id="personal-info-heading" className="mt-1 text-xl font-black text-slate-900">Personal Information</h2></div><button type="button" onClick={onEdit} className="rounded-xl bg-[var(--brand-primary)] px-4 py-2 text-sm font-bold text-white hover:bg-[var(--brand-primary-hover)]">Edit Information</button></div><div className="mt-5 flex flex-col gap-5 sm:flex-row"><label className="group relative h-24 w-24 shrink-0 cursor-pointer"><div className="flex h-24 w-24 items-center justify-center overflow-hidden rounded-2xl bg-[var(--brand-soft)] text-2xl font-black text-[var(--brand-deep)]">{profile.avatarUrl ? <img src={profile.avatarUrl} alt={`${profile.name || 'User'} avatar`} className="h-full w-full object-cover" /> : (profile.name || 'U').charAt(0).toUpperCase()}</div><span className="absolute inset-0 hidden items-center justify-center rounded-2xl bg-slate-900/60 text-white group-hover:flex"><Camera className="h-5 w-5" /></span><input type="file" accept="image/*" onChange={onAvatarChange} className="sr-only" aria-label="Upload profile avatar" /></label><div className="min-w-0 flex-1"><h3 className="text-lg font-black text-slate-900">{profile.name || 'Your name'}</h3><p className="mt-1 text-sm font-semibold text-[var(--brand-deep)]">{profile.headline || 'Add a professional headline'}</p><div className="mt-3 grid gap-2 text-sm text-slate-500 sm:grid-cols-2"><span><Mail className="mr-2 inline h-4 w-4" />{profile.email || 'Add your email'}</span><span><Phone className="mr-2 inline h-4 w-4" />{profile.phone || 'Add your phone'}</span><span><MapPin className="mr-2 inline h-4 w-4" />{profile.location || 'Add your location'}</span></div></div></div>{profile.bio && <p className="mt-5 border-t border-slate-100 pt-4 text-sm leading-6 text-slate-600">{profile.bio}</p>}<div className="mt-4 flex flex-wrap gap-3 text-sm font-semibold"><a href={profile.linkedin || '#'} onClick={(event) => !profile.linkedin && event.preventDefault()} className="text-[var(--brand-deep)] hover:underline">LinkedIn</a><a href={profile.github || '#'} onClick={(event) => !profile.github && event.preventDefault()} className="text-[var(--brand-deep)] hover:underline">GitHub</a></div></section>;
}
