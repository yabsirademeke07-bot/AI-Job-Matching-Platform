import { useState } from 'react';
export default function EditPersonalInfoModal({ profile, onCancel, onSave }) { const [form, setForm] = useState({ name: profile.name || '', phone: profile.phone || '', location: profile.location || '', dob: profile.dob || '' }); const update = (key, value) => setForm((current) => ({ ...current, [key]: value })); return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/50 p-4"><form onSubmit={(event) => { event.preventDefault(); onSave(form); }} className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><h2 className="text-xl font-black text-slate-900">Edit Personal Information</h2><div className="mt-5 grid gap-3">{[['name', 'Full Name'], ['phone', 'Phone'], ['location', 'Location']].map(([key, label]) => <label key={key} className="text-sm font-bold text-slate-700">{label}<input value={form[key]} onChange={(event) => update(key, event.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 font-normal" /></label>)}<label className="text-sm font-bold text-slate-700">Date of Birth (optional)<input type="date" value={form.dob} onChange={(event) => update('dob', event.target.value)} className="mt-1.5 min-h-11 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 font-normal" /></label></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={onCancel} className="min-h-11 rounded-xl border border-slate-200 px-4 font-bold text-slate-600">Cancel</button><button type="submit" className="min-h-11 rounded-xl bg-[var(--brand-primary)] px-5 font-bold text-white">Save Changes</button></div></form></div>; }
