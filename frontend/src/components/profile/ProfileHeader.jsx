import { Camera, MapPin } from 'lucide-react';
export default function ProfileHeader({ profile, completion, onEdit, onAvatarChange }) { const name = profile?.name || 'User'; return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><label className="group relative flex h-20 w-20 shrink-0 cursor-pointer items-center justify-center overflow-hidden rounded-2xl bg-[var(--brand-soft)] text-2xl font-black text-[var(--brand-deep)]">{profile?.avatarUrl ? <img src={profile.avatarUrl} alt={`${name} avatar`} className="h-full w-full object-cover" /> : name.charAt(0).toUpperCase()}<span className="absolute inset-0 hidden items-center justify-center bg-slate-900/60 text-white group-hover:flex"><Camera className="h-5 w-5" /></span><input type="file" accept="image/*" onChange={onAvatarChange} className="sr-only" aria-label="Upload avatar" /></label><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--brand-deep)]">Candidate profile</p><h1 className="mt-1 text-2xl font-black text-slate-900">{name}</h1><p className="mt-1 font-semibold text-[var(--brand-deep)]">{profile?.headline || 'Add your professional title'}</p><p className="mt-1 text-sm text-slate-500"><MapPin className="mr-1 inline h-4 w-4" />{profile?.location || 'Add your location'}</p></div></div><button type="button" onClick={onEdit} className="min-h-11 rounded-xl bg-[var(--brand-primary)] px-5 text-sm font-bold text-white hover:bg-[var(--brand-primary-hover)]">Edit Profile</button></div><div className="mt-6"><div className="flex justify-between text-sm font-bold text-slate-700"><span>Profile Completion</span><span className="text-[var(--brand-deep)]">{completion}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[var(--brand-primary)]" style={{ width: `${completion}%` }} /></div></div></section>; }
